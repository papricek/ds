<!DOCTYPE html>
<html lang="cs">
  <%= render "layouts/head" %>

  <body>
    <%= render "layouts/flash" %>
    <%= yield %>

    <div class="modal fade" id="modal-window" tabindex="-1">
      <div class="modal-dialog">
        <div class="modal-content" style="background: var(--brand-dark); border: 1px solid var(--white-1);"></div>
      </div>
    </div>
  </body>
</html>
