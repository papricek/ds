<% content_for :title, t("superadmin.accounts.new_account") %>

<div class="d-flex justify-content-between align-items-center mb-4">
  <h1 class="text-white mb-0"><%= t("superadmin.accounts.new_account") %></h1>
</div>

<div class="Card">
  <%= render "form", url: superadmin_accounts_path %>
</div>
