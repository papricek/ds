<% content_for :title, t("superadmin.users.new_user") %>

<div class="d-flex justify-content-between align-items-center mb-4">
  <h1 class="text-white mb-0"><%= t("superadmin.users.new_user") %></h1>
</div>

<div class="Card">
  <%= render "form", url: superadmin_account_users_path(@account) %>
</div>
